<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/page_title_relativeLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/shape_page_title_background" >

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true">

        <ImageView
            android:layout_marginLeft="15dp"
            android:layout_gravity="left"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:src="@drawable/ic_quit" />

        <Button
            android:id="@+id/cancel_button"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="right|center_vertical"
            android:paddingLeft="45dp"
            android:text="返回"
            android:background="@null"
            android:textColor="@drawable/book_shelf_selector_botton"
            android:textSize="18sp" />
    </FrameLayout>

    <TextView
        android:id="@+id/title_TextView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:textColor="@android:color/white"
        android:textSize="22sp" />

    <cn.retech.custom_control.SearchLayout
        android:id="@+id/search_SearchView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <ImageButton
        android:id="@+id/refresh_ImageButton"
        android:layout_width="60dp"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:background="@drawable/selector_button"
        android:src="@drawable/ic_update" />

    <ImageButton
        android:id="@+id/doSearch_ImageButton"
        android:layout_width="60dp"
        android:layout_height="match_parent"
        android:layout_toLeftOf="@id/refresh_ImageButton"
        android:background="@drawable/selector_button"
        android:src="@drawable/ic_query" />

</RelativeLayout>